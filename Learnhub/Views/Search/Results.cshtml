@model List<string>

<h2>Search Results</h2>

@if (Model.Count > 0)
{
    <ul>
        @foreach (var result in Model)
        {
            <li><a href="@Url.Action("Detail", "Course", new { title = result })">@result</a></li>
        }
    </ul>
}
else
{
    <p>No matching results found</p>
}
